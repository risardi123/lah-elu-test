import {StyleSheet} from 'react-native';
import {margin, paddingSize} from '../../../../components/config.ts';

export const styles = StyleSheet.create({
  scrollView: {
    margin: paddingSize.xl,
  },
  button: {
    marginLeft: margin.md,
  },
});
